---
import Badge from "./Badge.astro";
import ExternalLink from "@components/icons/ExternalLink.astro";
import GitHubIcon from "@components/icons/GitHub.astro";

export interface Props {
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
  tech: string[];
  deployUrl?: string;
  repoUrl?: string;
}

const { title, description, imageUrl, imageAlt, tech, deployUrl, repoUrl } =
  Astro.props;
---

<article
  class="group flex flex-col rounded-lg border border-border/60 bg-card p-5 transition-colors hover:border-primary/40 sm:p-6"
>
  <figure
    class="mb-4 overflow-hidden rounded-md border border-border/60 bg-background"
  >
    {
      deployUrl ? (
        <a
          href={deployUrl}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={`Ver proyecto ${title}`}
        >
          <img
            src={imageUrl}
            alt={imageAlt}
            loading="lazy"
            decoding="async"
            class="aspect-video h-full w-full object-cover"
          />
        </a>
      ) : (
        <img
          src={imageUrl}
          alt={imageAlt}
          loading="lazy"
          decoding="async"
          class="aspect-video h-full w-full object-cover"
        />
      )
    }
  </figure>

  <header class="mb-3">
    <h3 class="text-base font-semibold tracking-tight text-foreground mb-2.5">
      {title}
    </h3>

    <div class="flex flex-wrap gap-1.5">
      {
        tech.map((technology) => (
          <Badge variant="secondary">{technology}</Badge>
        ))
      }
    </div>
  </header>

  <p class="mb-4 text-sm leading-relaxed text-muted-foreground">
    {description}
  </p>

  {
    (repoUrl || deployUrl) && (
      <nav
        class="mt-auto flex flex-wrap gap-2"
        aria-label={`Project links for ${title}`}
      >
        {repoUrl && (
          <a
            href={repoUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 rounded-md border border-border/60 px-3 py-1.5 text-sm font-medium text-foreground transition-colors hover:bg-muted hover:text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
          >
            Código
            <GitHubIcon class="size-4" />
          </a>
        )}

        {deployUrl && (
          <a
            href={deployUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 rounded-md bg-primary px-3 py-1.5 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
          >
            <ExternalLink class="size-4" />
            Demo
          </a>
        )}
      </nav>
    )
  }
</article>
