---
import Badge from "./Badge.astro";
import Icon from "./Icon.astro";

export interface Props {
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  statusLabel: string;
  statusVariant: "primary" | "tertiary" | "secondary";
  techLabel: string;
}
---

<div
  class="p-6 sm:p-8 border border-white/5 rounded-lg bg-surface-container-low/30 hover:bg-surface-container-low hover:border-primary/30 transition-all group cursor-pointer"
>
  <div
    class="aspect-video w-full mb-5 sm:mb-6 rounded overflow-hidden border border-white/10 bg-surface-container-high relative group-hover:border-primary/30 transition-colors"
  >
    <img
      alt={Astro.props.imageAlt}
      class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-500"
      src={Astro.props.image}
    />
    <div class="absolute inset-0 bg-gradient-to-t from-surface/40 to-transparent"></div>
  </div>
  <div class="flex justify-between items-start mb-5 sm:mb-6">
    <div>
      <h4 class="font-sans text-on-surface mb-2">{Astro.props.title}</h4>
      <div class="flex gap-2">
        <Badge variant={Astro.props.statusVariant}>
          {Astro.props.statusLabel}
        </Badge>
        <Badge variant="secondary">{Astro.props.techLabel}</Badge>
      </div>
    </div>
    <Icon
      name="arrow_outward"
      size="text-[18px]"
      class="text-secondary/40 group-hover:text-primary group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-all duration-300"
    />
  </div>
  <p class="text-secondary text-sm leading-relaxed">
    {Astro.props.description}
  </p>
</div>
