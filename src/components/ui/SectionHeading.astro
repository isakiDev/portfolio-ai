---
export interface Props {
  label: string;
  className?: string;
}

const { label, className } = Astro.props;
const headingClass = className || "mb-12";
---

<h3 class={`font-mono text-xs tracking-wider leading-none font-medium text-muted-foreground ${headingClass}`}>
  {label}
</h3>
